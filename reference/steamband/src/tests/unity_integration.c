/* File: src/tests/unity_integration.c
 * Unity framework integration helpers
 */

#include "unity.h"

/* Unity requires setUp and tearDown functions to be defined.
 * These are called before and after each test.
 * We provide empty implementations by default - tests can override if needed.
 */
void setUp(void) {
    /* Set up test fixtures before each test */
    /* Empty by default - individual test files can override if needed */
}

void tearDown(void) {
    /* Clean up after each test */
    /* Empty by default - individual test files can override if needed */
}

