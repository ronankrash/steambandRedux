# Headless Godot tests live in `game/tests/run_headless_tests.gd`.

Run via:

```bash
./tools/run_ci_local.sh
```

or:

```bash
tools/bin/godot --headless --path game --script res://tests/run_headless_tests.gd
```
